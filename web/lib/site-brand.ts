/** Visitor-facing site name (Sinhala). Use in UI only — not for Sheets/API keys. */
export const SITE_DISPLAY_NAME = "ප්‍රාර්ථනා";

/** Roman transliteration for bilingual lockups in the UI. */
export const SITE_DISPLAY_NAME_EN = "Prarthana";

/** Sinhala first, then English in parentheses (visitor-facing). */
export const SITE_BRAND_LOCKUP_PLAIN = `${SITE_DISPLAY_NAME} (${SITE_DISPLAY_NAME_EN})`;

export const SITE_TAGLINE_EN = "Cancer awareness & community";
